The ``1-my_list`` module and task 1
======================

Using ``print_sorted`` methods of MyList subclass
-------------------

Importing the function from the module:
    >>> MyList = __import__('1-my_list').MyList

List with some elemets
     >>> my_list.extend([5, 2, 9, 7])
     >>> my_list.print_sorted()
     [2, 5, 7, 9]

List empty
     >>> my_list = MyList()
     >>> my_list.print_sorted()
     []

List already sorted
     >>> my_list.sort()
     >>> my_list.print_sorted()
     [2, 5, 7, 9]

Extra arguments:
      >>> my_list.print_sorted([2, 3])
      Traceback (most recent call last):
      TypeError: print_sorted() takes 1 positional argument but 2 were given

Module doctring:
       >>> m_doc = __import__('1-my_list').__doc__
       >>> len(m_doc) > 1
       True

Class docstring:
      >>> c_doc = __import__('1-my_list').MyList.__doc__
      >>> len(c_doc) > 1
      True

Method docstring:
       >>> m_doc = __import__('1-my_list').MyList.print_sorted.__doc__
       >>> len(m_doc) > 1
       True

List with negative numbers
     >>> my_list[:] = [-8, -220, -1, -49, -75]
     >>> my_list.print_sorted()
     [-220, -75, -49, -8, -1]
